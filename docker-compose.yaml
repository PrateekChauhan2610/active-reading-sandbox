version: "3.9"
services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      # Optional: Add your key here so users don't have to enter it in the UI
      - GEMINI_API_KEY=$Your API Key